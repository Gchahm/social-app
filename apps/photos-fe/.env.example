# Copy this file to `.env` or `.env.local` and set the API POST endpoint
# The value must be the absolute URL to your backend POST route that accepts uploads.
# Examples:
# - When running SAM locally (from apps/be): http://127.0.0.1:3000/spaces
# - After deploying to API Gateway: https://xxxxxxxxxx.execute-api.<region>.amazonaws.com/prod/spaces

VITE_POST_URL=https://arw61nk0bi.execute-api.us-east-1.amazonaws.com/prod/spaces
